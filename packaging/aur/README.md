# Arch Linux (AUR)

This folder contains a PKGBUILD for the latest git version.

Build locally:

```
cd packaging/aur
makepkg -si
```

This installs system-wide under `/usr` and updates icon/desktop caches.
